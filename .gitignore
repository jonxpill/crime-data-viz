# dependencies
node_modules/

# build output
dist/
dist-ssr/

# vite / tooling caches
.vite/
*.local

# logs
*.log
npm-debug.log*

# local machine settings (permission grants — per-machine, not shared)
.claude/settings.local.json

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# scratch / data work (kept out of the engine repo until baked)
scratchpad/
*.tmp
